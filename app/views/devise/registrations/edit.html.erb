<div class="container">

  <!-- header -->

  <h2 class="text-center mt-3">Changer de mot de passe</h2>
  <p>Votre email pour accéder à Claire's garden: <%= current_user.email %></p>


  <!-- update form -->
  <div>
      <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">

      <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
        <p>Currently waiting confirmation for: <%= resource.unconfirmed_email %></p>
      <% end %>
      <%= f.input :password,
                  label: "Nouveau mot de passe",
                  required: false,
                  input_html: { autocomplete: "new-password" } %>
      <%= f.input :password_confirmation,
                  label: "Confirmation du nouveau mot de passe",
                  required: false,
                  input_html: { autocomplete: "new-password" } %>
      <%= f.input :current_password,
                  label: "Mot de passe actuel",
                  hint: "Nécessaire pour enregistrer votre nouveau mot de passe",
                  required: true,
                  input_html: { autocomplete: "current-password" } %>
      <%= f.button :submit, "Mettre à jour", class: "button claire-garden-button" %>
      <%= link_to "Annuler", plants_path, class: 'btn button delete-button my-2' %>
    </div>
  </div>
</div>
  <% end %>



  <!-- cancel account -->
  <!-- <div> -->
    <!-- <h5>Effacer mon compte</h5> -->

    <!-- <p>Vous n'êtes pas satisfait(e)? <%#= link_to "Effacer mon compte", registration_path(resource_name), data: { confirm: "Vous êtes sûr?" }, method: :delete %></p> -->

    <%#= link_to "Retour à l'accueil", plants_path %>

  <!-- </div> -->
<!-- </div> -->
